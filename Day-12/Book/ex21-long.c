// long Type Modifier

//long means that the variable has a larger storage so it can hold a 
//larger value. it usually doubles the size

#include <stdio.h>

int main(int argc, char const *argv[])
{
	long int biiigNumber = 13098728234;

	printf("This happens to be a REALLY big number: %ld\n", biiigNumber);

	return 0;
}